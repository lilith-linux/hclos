const std = @import("std");
const linux = std.os.linux;
const chroot = @import("chroot_setup.zig");

// pub fn pre_install(allocator: std.mem.Allocator, file_path: []const u8, prefix: []const u8) !void {

// }
